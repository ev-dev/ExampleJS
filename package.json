{
  "name": "ExampleJS",
  "version": "1.0.0",
  "main": "scripts/main.js",
  "scripts": {
    "scss-compile": "node-sass -rw styles/dev -o styles/prod",
    "watch": "npm run scss-compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "require": "^2.4.20"
  },
  "devDependencies": {
    "node-sass": "^4.5.3",
    "scss-compile": "^0.1.7"
  }
}
